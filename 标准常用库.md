# 标准常用库

## `fmt`格式化

```go
package main

import "fmt"

func main() {
    // 1. 打印输出
    fmt.Println("Hello, World!")  // 自动换行
    fmt.Print("不换行")           // 不换行
    fmt.Printf("格式化: %s 年龄: %d\n", "张三", 20)
    
    // 2. 格式化字符串
    name := "李四"
    age := 25
    message := fmt.Sprintf("姓名: %s, 年龄: %d", name, age)
    fmt.Println(message)
    
    // 3. 输入（简单版）
    var input string
    fmt.Print("请输入你的名字: ")
    fmt.Scanln(&input)  // 读取一行输入
    fmt.Printf("你好, %s!\n", input)
}
```

## `os`文件操作

```go
package main

import (
    "fmt"
    "os"
)

func main() {
    // 1. 写入文件
    content := "Hello, 这是文件内容！\n第二行"
    err := os.WriteFile("test.txt", []byte(content), 0644)
    if err != nil {
        fmt.Println("写入失败:", err)
        return
    }
    fmt.Println("✅ 文件写入成功")
    
    // 2. 读取文件
    data, err := os.ReadFile("test.txt")
    if err != nil {
        fmt.Println("读取失败:", err)
        return
    }
    fmt.Printf("✅ 文件内容: %s\n", string(data))
    
    // 3. 检查文件是否存在
    if _, err := os.Stat("test.txt"); err == nil {
        fmt.Println("📁 文件存在")
    } else {
        fmt.Println("❌ 文件不存在")
    }
    
    // 4. 命令行参数
    fmt.Println("命令行参数:", os.Args)
}


os.Args 读取命令行，为一个切片
第一个是程序名，随后是参数

os.Open(filename)
打开文件,记得用Close关闭

// Scanner就像是一个"阅读器"，帮你逐行读取文件
scanner := bufio.NewScanner(file)
// scanner.Scan() 读取下一行
// scanner.Text() 获取当前行的内容

//用于一行一行地读取所有文本
for scanner.Scan() {
}

//用于知道line中的以空格隔开的词，比如说英语
len(strings.Fields(line))






```

## `io`输入输出

```go
package main

import (
    "fmt"
    "io"
    "os"
    "strings"
)

func main() {
    // 1. 从字符串读取
    reader := strings.NewReader("Hello from strings!")
    data, _ := io.ReadAll(reader)
    fmt.Printf("从字符串读取: %s\n", data)
    
    // 2. 复制数据（文件复制）
    src, _ := os.Open("source.txt")
    dst, _ := os.Create("destination.txt")
    io.Copy(dst, src)  // 复制文件内容src到dst
    fmt.Println("✅ 文件复制完成")
    
    // 3. 读取指定长度
    limitedReader := io.LimitReader(strings.NewReader("1234567890"), 5)
    limitedData, _ := io.ReadAll(limitedReader)
    fmt.Printf("只读前5个字符: %s\n", limitedData)
}
```

## 



## `json`前后端交互

```go
package main

import (
    "encoding/json"
    "fmt"
)

// 定义结构体（对应JSON结构）
type User struct {
    ID       int    `json:"id"`
    Name     string `json:"name"`
    Email    string `json:"email"`
    IsActive bool   `json:"is_active"`
}

func main() {
    // 1. 结构体转JSON（序列化）
    user := User{
        ID:       1,
        Name:     "张三",
        Email:    "zhangsan@example.com",
        IsActive: true,
    }
    
    jsonData, err := json.Marshal(user)
    if err != nil {
        fmt.Println("JSON编码失败:", err)
        return
    }
    fmt.Println("结构体转JSON:")
    fmt.Println(string(jsonData))
    
    // 2. JSON转结构体（反序列化）
    jsonString := `{"id":2,"name":"李四","email":"lisi@test.com","is_active":false}`
    var user2 User
    err = json.Unmarshal([]byte(jsonString), &user2)
    if err != nil {
        fmt.Println("JSON解码失败:", err)
        return
    }
    fmt.Println("\nJSON转结构体:")
    fmt.Printf("ID: %d, 姓名: %s, 邮箱: %s, 活跃: %t\n", 
        user2.ID, user2.Name, user2.Email, user2.IsActive)
    
    // 3. 处理动态JSON（不知道结构时）
    dynamicJSON := `{"name":"王五","age":25,"hobbies":["篮球","读书"]}`
    var data map[string]interface{}
    json.Unmarshal([]byte(dynamicJSON), &data)
    
    fmt.Println("\n动态JSON解析:")
    fmt.Println("姓名:", data["name"])
    fmt.Println("年龄:", data["age"])
    fmt.Println("爱好:", data["hobbies"])
}
```

